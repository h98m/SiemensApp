<UserControl x:Class="SiemensApp.Views.InvoicesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F8FAFC" FlowDirection="RightToLeft"
             FontFamily="Segoe UI Semibold">

    <UserControl.Resources>
        <Style x:Key="ModernDataGridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F1F5F9"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#F0F7FF"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#1E293B"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#F0F7FF"/>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8FAFC"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#F0F7FF"/>
                    <Setter Property="BorderBrush" Value="#6366F1"/>
                    <Setter Property="BorderThickness" Value="0,0,4,1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Foreground}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel HorizontalAlignment="Right">
                <TextBlock Text="أرشيف الفواتير" FontSize="30" FontWeight="Black" Foreground="#0F172A"/>
                <TextBlock Text="عرض وتدقيق فواتير المبيعات السابقة" FontSize="14" Foreground="#64748B" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Border Background="White" CornerRadius="12" Width="380" Height="48" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Grid>
                        <TextBox x:Name="txtSearchInvoice" Background="Transparent" BorderThickness="0" Padding="45,0,15,0" VerticalContentAlignment="Center" FontSize="15" TextChanged="txtSearchInvoice_TextChanged"/>
                        <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0" Foreground="#94A3B8" IsHitTestVisible="False"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="15" Padding="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>

            <DataGrid x:Name="dgvInvoices" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      Background="Transparent" 
                      BorderThickness="0" 
                      RowHeight="70"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      RowStyle="{StaticResource ModernDataGridRow}"
                      CellStyle="{StaticResource ModernCellStyle}"
                      ColumnHeaderStyle="{StaticResource ModernHeader}">

                <DataGrid.Columns>
					<DataGridTextColumn Header="رقم الوصل" Binding="{Binding InvoiceNumber}" Width="100">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="HorizontalAlignment" Value="Center"/>
								<Setter Property="VerticalAlignment" Value="Center"/>
								<Setter Property="FontWeight" Value="Bold"/>
								<Setter Property="Foreground" Value="#4F46E5"/>
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>

                    <DataGridTemplateColumn Header="الزبون" Width="3*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Margin="20,0">
                                    <TextBlock Text="{Binding CustomerName}" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                                    <TextBlock Text="{Binding Phone}" FontSize="12" Foreground="#94A3B8" Margin="0,3,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat={}{0:yyyy/MM/dd}}" Width="1.5*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="إجمالي الفاتورة" Width="1.8*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F8FAFC" CornerRadius="10" Padding="12,8" HorizontalAlignment="Center" BorderBrush="#F1F5F9" BorderThickness="1">
                                    <StackPanel VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N0}}" FontSize="14" FontWeight="Bold" Foreground="#0F172A"/>
                                            <TextBlock Text=" د.ع" FontSize="11" Foreground="#64748B" VerticalAlignment="Bottom" Margin="4,0,0,2"/>
                                        </StackPanel>

                                        <Rectangle Height="1" Fill="#E2E8F0" Margin="0,4"/>

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding TotalAmountDollar, StringFormat={}{0:N2}}" FontSize="14" FontWeight="Bold" Foreground="#15803D"/>
                                            <TextBlock Text=" $" FontSize="12" Foreground="#15803D" VerticalAlignment="Bottom" Margin="4,0,0,2"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="العمليات" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Click="BtnViewDetails_Click" Cursor="Hand" Background="Transparent" BorderThickness="0">
                                        <Border Background="#F1F5F9" CornerRadius="10" Padding="8,6">
                                            <TextBlock Text="📄 تفاصيل" FontSize="12" FontWeight="Bold" Foreground="#475569"/>
                                        </Border>
                                    </Button>

                                    <Button Click="BtnDeleteInvoice_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" Margin="5,0,0,0">
                                        <Border Background="#FEF2F2" CornerRadius="10" Padding="8,6">
                                            <TextBlock Text="🗑️ حذف" FontSize="12" FontWeight="Bold" Foreground="#EF4444"/>
                                        </Border>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>